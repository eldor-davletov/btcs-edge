spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/btcs
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

#  flyway:
#    enabled: true
#    default-schema: public
#
#  logging:
#    level:
#      org.flywaydb: INFO